{
  "scripts": {
    "deploy": "npm run prep && surge -p ./dist",
    "start": "npm run prep && python -m SimpleHTTPServer",
    "prep": "npm run babel && cp index.html dist/ && cp style.css dist/",
    "babel": "babel app.js -d dist",
    "watch": "babel app.js -w -d dist --source-maps inline"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0"
  }
}
