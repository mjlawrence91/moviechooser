{
  "scripts": {
    "deploy": "surge",
    "start": "npm run babel && python -m SimpleHTTPServer",
    "babel": "babel app.js -d dist",
    "watch": "babel app.js -w -d dist --source-maps inline"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0"
  }
}
