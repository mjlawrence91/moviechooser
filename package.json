{
  "scripts": {
    "start": "npm run deploy && node server",
    "deploy": "npm run babel && sh deploy.sh",
    "babel": "babel App.js -d dist --minified",
    "dev": "nodemon --watch server.js --exec 'NODE_ENV=dev node server'",
    "watch": "babel App.js -w -d dist --source-maps inline"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-eslint": "6.0.2",
    "babel-preset-es2015": "^6.9.0",
    "eslint": "2.9.0",
    "eslint-config-standard": "^5.3.1",
    "eslint-plugin-promise": "^1.1.0",
    "eslint-plugin-standard": "^1.3.2",
    "nodemon": "^1.10.0"
  },
  "dependencies": {
    "express": "4.14.0",
    "serve-static": "1.11.1"
  }
}
